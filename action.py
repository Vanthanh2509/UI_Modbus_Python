# def connection(self):
#         # client = ModbusSerialClient(method = str(self.type.currentText()), port= str(self.Port.currentText()),stopbits = int(self.stop_bit.currentText()), bytesize = 8, parity = 'N', baudrate= 9600)
#         print('thanh')

#  self.Port.addItems(data._comport)
#         self.type.addItems(data.type_connect)
#         self.baudrate.addItems(data.baudrate)
#         self.byte_size.addItems(data.data_bit)
#         self.stop_bit.addItems(data.stop_bit)
#         self.parity.addItems(data.parity)

# self.Read_Sl_6.clicked.connect(self.connection)